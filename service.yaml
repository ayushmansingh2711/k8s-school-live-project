apiVersion: v1
kind: Service
metadata:
  name: react-service
spec:
  type: NodePort   # change to LoadBalancer if on cloud
  selector:
    app: react-app
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30005   # custom NodePort
